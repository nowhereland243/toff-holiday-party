# Tom of Finland Foundation 网站技术解析教程

## 目录
1. [整体架构](#整体架构)
2. [核心技术栈](#核心技术栈)
3. [视觉效果实现](#视觉效果实现)
4. [交互功能](#交互功能)
5. [响应式设计](#响应式设计)
6. [性能优化](#性能优化)

---

## 整体架构

这个网站是一个**单页应用（SPA）**，所有内容都在一个 HTML 文件中，通过滚动展示不同的内容区块。

### 文件结构
```
v7/
├── index_v3.html        # 主HTML文件
├── styles/
│   └── style.css        # 主样式表
├── scripts/
│   └── theme.js         # 主JavaScript文件（WebGL动画）
├── audio/
│   └── ambient-audio.mp3# 背景音乐
└── assets/              # 图片、图标等资源
```

---

## 核心技术栈

### 1. **HTML5**
- 语义化标签：`<section>`, `<header>`, `<nav>` 等
- Meta标签：用于SEO和社交媒体分享（Open Graph）

```html
<!-- SEO优化 -->
<meta name="description" content="网站描述">
<meta property="og:title" content="分享标题">
<meta property="og:image" content="分享图片">
```

### 2. **CSS**
- **自定义属性（CSS Variables）**：统一管理颜色、间距
- **Flexbox布局**：响应式布局
- **媒体查询（Media Queries）**：适配不同屏幕尺寸
- **过渡动画（Transitions）**：流畅的hover效果

```css
/* 移动端适配 */
@media (max-width: 767px) {
    #mission { margin-top: 100vh !important; }
}
```

### 3. **JavaScript (ES6+)**
- **WebGL**：3D背景动画（花朵绽放效果）
- **GSAP (GreenSock)**：动画库，用于平滑滚动动画
- **模块化（ES Modules）**：`type="module"` 导入

```javascript
// 模块化导入
<script type="module" src="scripts/theme.js"></script>
```

### 4. **第三方工具**
- **Google Analytics**：用户行为追踪
- **Vercel**：网站托管和部署

---

## 视觉效果实现

### 1. **背景花朵动画（核心特效）**

这是网站最炫酷的部分！使用 **WebGL + 图片序列** 实现。

**工作原理：**
1. 准备158张连续的花朵图片（`manifesto000.jpg` 到 `manifesto157.jpg`）
2. 根据用户滚动进度，切换显示不同的图片
3. 配合 WebGL 着色器（Shader）实现3D效果和色彩滤镜

**技术细节：**
```javascript
// 伪代码示例
const images = []; // 存储所有花朵图片
let currentFrame = 0;

window.addEventListener('scroll', () => {
    // 计算滚动百分比
    const scrollPercent = window.scrollY / (document.height - window.height);
    
    // 根据滚动百分比选择对应的图片帧
    currentFrame = Math.floor(scrollPercent * images.length);
    
    // 渲染到WebGL画布
    renderFrame(images[currentFrame]);
});
```

### 2. **深色主题（Dark Mode）**

```html
<!-- body标签上的class控制主题 -->
<body class="dark:bg-black bg-off-white">
```

- 使用 `dark:` 前缀的类名（类似Tailwind CSS）
- CSS中使用 `html:not(.dark)` 选择器切换样式

### 3. **文字渐入动画**

使用 GSAP 的 `ScrollTrigger` 插件：
- 文字在滚动到可视区域时渐入
- 配合 `opacity` 和 `transform` 实现

```css
.js-manifesto:text {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease;
}

.js-manifesto:text.is-visible {
    opacity: 1;
    transform: translateY(0);
}
```

---

## 交互功能

### 1. **音频控制**

```javascript
// 点击任意位置启用音频
document.addEventListener('click', () => {
    audioElement.play();
    // 隐藏"点击启用音频"提示
    document.querySelector('.js-manifesto:audioNotice').hide();
}, { once: true }); // 只执行一次
```

### 2. **可折叠研究领域（Accordion）**

```html
<div class="research-toggle | js-research-toggle">
    <div class="research-toggle__header">
        <strong>Queer History</strong>
        <div class="research-toggle__icon"></div>
    </div>
    <div class="research-toggle__content">
        详细内容...
    </div>
</div>
```

**CSS技巧：**
```css
.research-toggle__content {
    max-height: 0;           /* 初始高度为0 */
    overflow: hidden;        /* 隐藏溢出内容 */
    opacity: 0;
    transition: all 0.4s;
}

.research-toggle.is-open .research-toggle__content {
    max-height: 200px;       /* 展开时设置最大高度 */
    opacity: 1;
}
```

**JavaScript：**
```javascript
document.querySelectorAll('.js-research-toggle').forEach(toggle => {
    toggle.addEventListener('click', () => {
        toggle.classList.toggle('is-open');
    });
});
```

### 3. **加号/减号图标动画**

```css
/* 使用伪元素制作加号 */
.research-toggle__icon::before {
    content: '';
    width: 100%;
    height: 2px;
    background: white;
    /* 横线 */
}

.research-toggle__icon::after {
    content: '';
    width: 2px;
    height: 100%;
    background: white;
    /* 竖线 */
}

/* 点击展开时，竖线旋转90度变成减号 */
.research-toggle.is-open .research-toggle__icon::after {
    transform: rotate(90deg);
}
```

---

## 响应式设计

### 1. **断点（Breakpoints）**

```css
/* 小屏幕（手机） */
@media (max-width: 767px) {
    .container { padding: 20px; }
}

/* 中等屏幕（平板） */
@media (min-width: 768px) and (max-width: 1023px) {
    .container { padding: 40px; }
}

/* 大屏幕（桌面） */
@media (min-width: 1024px) {
    .container { padding: 60px; }
}
```

### 2. **移动优先（Mobile-First）**

网站代码先写移动端样式，再用 `@media` 添加桌面端样式：

```css
/* 默认（移动端） */
.title { font-size: 32px; }

/* 桌面端覆盖 */
@media (min-width: 1024px) {
    .title { font-size: 64px; }
}
```

### 3. **移动端特殊处理**

```css
/* 手机上给每个section之间加100vh空隙 */
@media (max-width: 767px) {
    #mission,
    #programs,
    #financials,
    #partners {
        margin-top: 100vh !important;
    }
}
```

---

## 性能优化

### 1. **图片优化**
- 使用 WebP 格式（体积更小）
- 序列帧图片预加载

```javascript
// 预加载所有花朵图片
const preloadImages = (urls) => {
    urls.forEach(url => {
        const img = new Image();
        img.src = url;
    });
};
```

### 2. **懒加载（Lazy Loading）**

```html
<!-- 图片懒加载，滚动到附近才加载 -->
<img loading="lazy" src="image.jpg" alt="描述">
```

### 3. **防抖（Debounce）滚动事件**

```javascript
// 避免滚动事件触发太频繁
let scrollTimeout;
window.addEventListener('scroll', () => {
    clearTimeout(scrollTimeout);
    scrollTimeout = setTimeout(() => {
        updateAnimation();
    }, 16); // 约60fps
});
```

### 4. **CSS优化**
- 使用 `transform` 而非 `top/left`（GPU加速）
- 避免复杂的选择器

```css
/* ❌ 慢 */
.container .item .title { ... }

/* ✅ 快 */
.item-title { ... }
```

---

## 关键代码解析

### 1. **全屏Section**

```html
<section class="h-100vh d-flex justify-center items-center">
    <h1>标题</h1>
</section>
```

```css
.h-100vh { height: 100vh; }  /* 100%视口高度 */
.d-flex { display: flex; }
.justify-center { justify-content: center; }
.items-center { align-items: center; }
```

### 2. **居中布局**

```css
/* Flexbox居中 */
.container {
    display: flex;
    justify-content: center;  /* 水平居中 */
    align-items: center;      /* 垂直居中 */
}
```

### 3. **背景渐变**

```css
body {
    background: linear-gradient(
        135deg,          /* 对角线方向 */
        #000000 0%,      /* 起始颜色 */
        #1a1a1a 100%     /* 结束颜色 */
    );
}
```

---

## 学习建议

### 初学者路径：
1. **从HTML开始**：理解页面结构
2. **学CSS基础**：Flexbox、Grid、响应式
3. **学JavaScript基础**：DOM操作、事件监听
4. **进阶**：WebGL、动画库（GSAP）

### 推荐资源：
- **MDN Web Docs**：https://developer.mozilla.org/zh-CN/
- **CSS-Tricks**：https://css-tricks.com/
- **GSAP文档**：https://greensock.com/docs/

### 实践练习：
1. **复制简单效果**：先从可折叠菜单开始
2. **改造现有网站**：修改颜色、间距、文字
3. **从零构建**：用学到的技术做自己的项目

---

## 总结

这个网站的核心亮点：
1. ✨ **WebGL花朵动画**：视觉冲击力强
2. 🎵 **背景音乐**：沉浸式体验
3. 📱 **响应式设计**：手机和桌面都完美适配
4. ⚡ **流畅动画**：GSAP + 滚动触发

**技术栈总结：**
- HTML5 + CSS3 + JavaScript ES6+
- WebGL + GSAP
- 模块化架构
- 移动优先设计理念

如果你有任何具体问题，随时问我！💪
